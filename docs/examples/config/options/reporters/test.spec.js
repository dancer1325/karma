// eslint-disable-next-line no-undef
describe('Progress Reporter Test', function () {
  // eslint-disable-next-line no-undef
  it('should pass', function () {
    // eslint-disable-next-line no-undef
    expect(true).toBe(true)
  })

  // eslint-disable-next-line no-undef
  it('should also pass', function () {
    // eslint-disable-next-line no-undef
    expect(1 + 1).toBe(2)
  })

  // eslint-disable-next-line no-undef
  it('should fail', function () {
    // eslint-disable-next-line no-undef
    expect(false).toBe(true)
  })
})
